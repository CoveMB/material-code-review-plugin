#!/usr/bin/env bash
set -euo pipefail

PACKAGE_ROOT="$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"
exec python3 "$PACKAGE_ROOT/skills/material-code-review/scripts/reviewctl.py" "$@"
