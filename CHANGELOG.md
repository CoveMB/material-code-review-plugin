# Changelog

## 1.2.0 — 2026-07-26

- Bound coverage to controller-owned workflow profiles so material review retains its core roster while every material-simplification selector uses required architecture/structural and code/test preflight waves and fails closed on missing evidence.
- Added a first-class GitHub pull-request scope that binds exact host provenance separately from its effective merge-base-to-head diff, retains ordinary direct-range compatibility, and fails closed before run creation on incomplete or mismatched metadata.
- Added required protocol-aware coverage and one bounded candidate preflight correction, and made missing required coverage fail closed as `REVIEW_INCOMPLETE` while retaining the existing low-value-advice suppression.
- Separated primary correction and required-lens fallback into explicit route-local receipt paths and budgets, so a correction cannot consume fallback authority and neither route can resume or supersede the other.
- Bound every activated fallback to an immutable root-owned assignment containing the exact failure trigger and actual reviewer identity, and propagated its degraded provenance through preflight, normalized candidates, coverage, and independence checks.
- Added root-owned no-output reviewer attestations and zero-input exhausted-coverage finalization, with controlled numeric diagnostics and no fabricated candidate data, raw logs, merge verdict, or gate.
- Added provisional grouping followed by a mandatory, hash-bound repair-direction audit for every retained finding.
- Added adjudication/v3 and ledger/v3 audit provenance, including accurate independent, controller-direct, and degraded modes.
- Added fix-plan/v2 with exact approved-direction hashes, complete constraint/state/decision handling, alternatives, and explicit divergence rationale.
- Added final-state refresh of exact required finding tests and an evidence-hash-bound pre-verification recovery transition, closing overlapping-path and failed-global-test deadlocks without granting generic reopen authority or new repair budgets.
- Aligned material simplification with the shared audit and plan lifecycle while preserving behavior characterization, net-reduction, and bounded-rewrite rules.
- Added the independently versioned material-simplification 1.1.0 archive with its required shared controller, schemas, and remediation references.
- Aligned Codex and Claude plugin manifests, validators, packagers, archive identities, and current-capability documentation.

## 1.1.0 — 2026-07-17

- Added a root Agent Skills `SKILL.md` entrypoint for Codex and OpenAI Skills import.
- Added Codex-specific host adaptation and `AGENTS.md` package guidance.
- Completed all previously referenced schemas, prompts, agents, command wrapper, controller wrapper, validation tooling, licensing, and documentation.
- Added deterministic full-package and standalone Codex-skill packaging.
- Added package-integrity checks for missing references and archive-root compatibility.
- Corrected the documented test count to 19.

## 1.0.0

- Initial frozen-scope, two-gate material review controller and lifecycle test suite.
