# Examples

The controller tests under `../tests/test_reviewctl.py` contain executable examples of valid candidate, adjudication, fix-plan, and verification payloads. They are preferred over static sample hashes because each test creates a real frozen repository and evidence bundle.
