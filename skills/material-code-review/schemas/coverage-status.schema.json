{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://local.invalid/material-code-review/coverage-status.schema.json",
  "title": "Material Code Review coverage status",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "scope_hash",
    "coverage_plan_hash",
    "workflow_profile",
    "status",
    "lenses",
    "limitations",
    "coverage_status_hash"
  ],
  "properties": {
    "schema_version": {
      "const": "material-review/coverage-status/v1"
    },
    "scope_hash": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "coverage_plan_hash": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "workflow_profile": {
      "enum": ["material_review", "material_simplification"]
    },
    "status": {
      "enum": ["complete", "incomplete"]
    },
    "lenses": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "lens_id",
          "required",
          "completed",
          "completion_route",
          "primary_receipt_hash",
          "fallback_receipt_hash",
          "primary_attestation_hash",
          "fallback_attestation_hash",
          "fallback_assignment_hash",
          "fallback_trigger_kind",
          "fallback_trigger_hash",
          "degraded",
          "evidence_handling",
          "reviewer_id",
          "independence_group",
          "review_mode",
          "diagnostics"
        ],
        "properties": {
          "lens_id": {
            "type": "string",
            "minLength": 1
          },
          "required": {
            "type": "boolean"
          },
          "completed": {
            "type": "boolean"
          },
          "completion_route": {
            "type": ["string", "null"],
            "enum": ["primary", "fallback", null]
          },
          "primary_receipt_hash": {
            "type": ["string", "null"],
            "pattern": "^[0-9a-f]{64}$"
          },
          "fallback_receipt_hash": {
            "type": ["string", "null"],
            "pattern": "^[0-9a-f]{64}$"
          },
          "primary_attestation_hash": {
            "type": ["string", "null"],
            "pattern": "^[0-9a-f]{64}$"
          },
          "fallback_attestation_hash": {
            "type": ["string", "null"],
            "pattern": "^[0-9a-f]{64}$"
          },
          "fallback_assignment_hash": {
            "type": ["string", "null"],
            "pattern": "^[0-9a-f]{64}$"
          },
          "fallback_trigger_kind": {
            "type": ["string", "null"],
            "enum": ["candidate_preflight_receipt", "reviewer_failure_attestation", null]
          },
          "fallback_trigger_hash": {
            "type": ["string", "null"],
            "pattern": "^[0-9a-f]{64}$"
          },
          "degraded": {
            "type": "boolean"
          },
          "evidence_handling": {
            "enum": ["standard", "unparseable_origin_degraded"]
          },
          "reviewer_id": {
            "type": "string",
            "minLength": 1
          },
          "independence_group": {
            "type": "string",
            "minLength": 1
          },
          "review_mode": {
            "enum": ["subagent", "controller", "external"]
          },
          "diagnostics": {
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1
            }
          }
        }
      }
    },
    "limitations": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "coverage_status_hash": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    }
  }
}
